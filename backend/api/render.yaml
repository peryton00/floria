services:
  - type: web
    name: floria-api
    env: node
    region: singapore
    plan: starter
    buildCommand: pnpm install && pnpm --filter @floria/api build
    startCommand: pnpm --filter @floria/api start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_ANON_KEY
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: CORS_ALLOWED_ORIGINS
        sync: false
